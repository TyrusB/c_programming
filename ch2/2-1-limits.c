#include <limits.h>
#include <stdio.h>

int main()
{
  printf("The max size of a char is %d\n", CHAR_MAX);
  printf("The max size of a short is %d\n", SHRT_MAX);
  printf("The max size of a int is %ld\n", INT_MAX);
  printf("The max size of a long is %ld\n", LONG_MAX);
}
